#ifndef DATA_H
#define DATA_H

#include "database.h"

// These functions can be used to add data to Database.

void AddUsers(Database *db);
void AddCategories(Database *db);
void AddSubcategories(Database *db);

#endif // DATA_H
